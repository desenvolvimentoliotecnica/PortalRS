@page "{id:guid}"
@model LiotecnicaHub.Web.Pages.Admin.Systems.DeleteModel
@{
    ViewData["Title"] = "Excluir sistema";
}

<section class="page-header">
    <h1>Excluir sistema</h1>
</section>

@if (Model.System is null)
{
    <p>Sistema não encontrado.</p>
}
else
{
    <div class="form-card">
        <p>Confirma a exclusão do sistema <strong>@Model.System.Name</strong> (<code>@Model.System.Code</code>)?</p>

        @if (Model.IsBuiltIn)
        {
            <p class="alert alert-error">Este é um sistema padrão e não pode ser excluído.</p>
        }
        else
        {
            <form method="post">
                <div asp-validation-summary="ModelOnly" class="alert alert-error"></div>
                <div class="form-actions">
                    <button type="submit" class="btn btn-danger">Excluir</button>
                    <a asp-page="Index">Cancelar</a>
                </div>
            </form>
        }
    </div>
}

<p><a asp-page="Index">← Voltar à lista</a></p>
