# Backlog

Tarefas pendentes, em andamento e conhecidas. Status:
- `[ ]` pendente
- `[~]` em andamento
- `[x]` concluído
- `[!]` bloqueado

---

## Em andamento

_(nenhum item)_

## Sessão 31.2 (2026-04-23) — Consolidação Area + Department → CentroCusto

- [x] **Unificação de domínio — `Area` + `Department` colapsam em `CentroCusto`** (2026-04-23): a Sessão 31.1 expôs que os três conceitos representam a mesma coisa no domínio real ("centro de custo"/"área" são intercambiáveis para o usuário). Rejeitado o MVP de "só resolver a rota quebrada" (regra de memória *"entregar completo, não MVP"*) e feita a consolidação completa:
  - **Backend entidade**: `CentroCusto` estendido com `Description2`, `Headcount`, `Phone` (campos do antigo `Department`). Entidades `Area`, `Department`, `Manager` removidas.
  - **Backend interface**: `ICurrentUserContext.AreaId` / `.IsGestorWithArea` → `.CentroCustoId` / `.IsGestorWithCentroCusto`.
  - **Backend contratos**: `VagaCreateRequest` / `VagaUpdateRequest` perdem `AreaId` e `DepartmentId`, mantêm só `CentroCustoId?`. `VagaListQuery` de 5 args para 4. `VagaListItemResponse.AreaId` → `CentroCustoId` + `CentroCustoCode` + `CentroCustoNome`. `FuncionarioListQuery` de 5 args para 4. `SolicitacaoVaga.AreaId` → `CentroCustoId`. `JobPositionResponse` e `SolicitacaoVagaResponse` alinhados (eliminada duplicação histórica de `CentroCustoId` em A.RH.013).
  - **Backend migration**: `20260423_ConsolidarAreasDepartamentosEmCentroCusto` — idempotente, com `INSERT ... WHERE NOT EXISTS` para copiar `Areas` preservando IDs; `UPDATE ... SET CentroCustoId = AreaId WHERE AreaId IS NOT NULL AND CentroCustoId IS NULL` para remapear FKs em 5 tabelas; `DROP TABLE IF EXISTS` para remover `Areas` e `Departments`. Sobrevive a re-execução em tenants em estágios diferentes.
  - **Backend seeder**: `CentroCustoSeeder` (novo) substitui `AreaSeeder` + `DepartmentSeeder` em `TenantProvisioningService`.
  - **Backend alias**: `LookupController` preserva `/areas` e `/departments` retornando `CentroCusto` no shape legado — ponte transitória para não quebrar consumidores internos ainda em migração.
  - **Backend services refatorados (~25)**: JobPositionService, SolicitacaoVagaService, PublicVagasController, DevSeedController, ReportsController, InboxFileProcessor, InboxController, CandidatosController, CandidatoService, CartaService, ListVagasPendenciasRhHandler, PreAdmissaoService, AgendaEventSeeder, DashboardController, ColaboradorService, NineBoxService, UserAdministrationService, TenantProvisioningService, FeriasService, DesligamentoService, TotvsIntegrationService, PublicCandidaturasController, ListFuncionariosHandler, OwnerController.
  - **Frontend redirects**: `/app/areas` e `/app/departamentos` → redirect client-side para `/app/centros-custo`. `AreasScreen.tsx` + `DepartamentosScreen.tsx` removidas (órfãs).
  - **Frontend global**: `GlobalSearchDialog` consolida categoria única "Centros de Custo"; `screenCache.PREFETCH_MAP` atualizado; `AdminAccessesScreen` com links novos; toda ocorrência de `areaId` / `departmentId` em screens + api clients renomeada.
  - **Frontend fix** (TS errors destravados): `AprovacoesScreen` — `SolicitacaoDetail` tinha `centroCustoId` duplicado (leftover do rename de `areaId` + A.RH.013) — mesclado em um par único. `SolicitacoesScreen` — `areaName`/`centroCustoName` em vários lugares alinhados para `centroCustoNome` (camelCase correto do backend).
  - **Testes** (38 arquivos ajustados): `DepartmentServiceTests` + pasta removidos. `DashboardAgregadoServiceTests`, `DocumentacaoPadrao*Tests`, `VagaCarteiraScopeTests`, `VagaServiceOperacoesTests`, `CriarVagaServiceTests`, `VagaFaixaSalarialTests`, `FuncionarioServiceTests`, `SolicitacaoVagaServiceTests`, `JobPositionServiceTests` atualizados com `CentroCusto` + assinaturas encolhidas de `VagaListQuery` / `FuncionarioListQuery`. `NavegacaoSidebarServiceTests` atualizado com novo bucket de cadastros (asserções em `/centros-custo` + `DoesNotContain` dos hrefs legados).
  - **Sidebar (`NavegacaoManifest`)**: itens `nav-departamentos` e `nav-areas` removidos. `nav-centros-custo` promovido para ordem 20 (posição que "Departamentos" ocupava no bloco Cadastros) — acesso único à entidade unificada em vez de três cliques diferentes para o mesmo destino.
  - **Grid unificado em `/centros-custo`**: `CentroCustoCadastroScreen.tsx` ganhou colunas de Headcount, Responsável (prefere `ownerFuncionarioName` e cai para `manager` legado), Filial/Local (`branchOrLocation`) e KPI "Headcount total" (somatório). Subtítulo atualizado explicitando que absorveu Área + Departamento. Sort corrigido para comparar `headcount` numericamente. Grid passou de 8 → 11 colunas.
  - **Fix — `ApplyOrphanMigrationsAsync` crashava API em DBs pós-31.2**: script histórico `ALTER TABLE "Vagas" ALTER COLUMN "DepartmentId" DROP NOT NULL` no `TenantProvisioningService` falhava com 42703 (coluna dropada pela migration de consolidação). Envolvido em `DO $$ IF EXISTS (information_schema.columns WHERE column_name = 'DepartmentId')` — idempotente: normaliza em DBs antigos (coluna presente), skip em DBs novos. Descoberto ao reiniciar a API na sessão — crash imediato com exit 134.
- [x] **Regressão — 762/762 testes verdes** (2026-04-23): `dotnet build` (test project) 0 erros / 38 warnings pré-existentes; `dotnet test` 762 aprovados / 0 falhos em 2s; `npx tsc --noEmit` 0 erros após fix de 3; `npx eslint` 0 erros novos nos arquivos tocados (pré-existentes 109 no repo inteiro são fora do escopo de 31.2).
- [x] **Fix — DELETE de Centro de Custo vinculado devolvia mensagem genérica** (2026-04-23): bug reportado pelo usuário — "tenho o TI que não consigo deletar e ele dá mensagem de erro mas não deixa claro o porquê". Raiz: `CentroCustoController.Delete` só validava filhos na hierarquia; quando havia vínculo em qualquer outra tabela (7 FKs: filhos, vagas, cargos, funcionários, solicitações de vaga, solicitações de promoção, pré-admissões), o `SaveChanges` quebrava com `23503 foreign_key_violation` e o frontend só exibia "Erro ao remover". Refeito com pre-check de todas as 7 FKs + safety net `DbUpdateException where SqlState == "23503"`; frontend passou a parsear o `message` do 409 e exibir literal ("Não é possível excluir 'TI - Tecnologia' — há vínculos: 2 vaga(s), 15 funcionário(s)..."). Body do 409 também traz `dependencies` estruturado para UI futura de "ver vínculos". 8 testes novos em `CentroCustoDeleteTests.cs`.
- [ ] **(follow-up)** Remover os aliases de `LookupController` (`/areas` e `/departments`) após varrer o código frontend + integrações que ainda consomem os paths antigos. Uma varredura por `apiClient.areas` + `apiClient.departments` nos screens tem que zerar antes.
- [ ] **(follow-up)** Revisar DTOs públicos/externos que ainda exibem `AreaNome` / `HeadcountPorArea` (endpoints de relatórios, públicos, portal de candidatos) — decidir se renomeia com versioning de API ou mantém alias indefinidamente.
- [ ] **(follow-up)** Endurecer os testes de solicitação + aprovação com cobertura explícita do campo `centroCustoNome` (tests já existem para `centroCustoId` mas não afirmam a string name serializada corretamente).
- [ ] **(follow-up)** Limpeza residual da permissão `departments.view`: ainda aparece em `RolePermissionManifest.cs`, `MenuSeeder.cs` (`/Departamentos` no portal MVC legado) e `MenuAdministrationService.cs`. Não quebra nada hoje (sidebar do Next já não usa), mas convém remover numa faxina quando o portal MVC for descomissionado de vez — tabela `RolePermissions` em prod precisa de DELETE idempotente para não deixar permissão órfã nos roles.

## Sessão 31.1 (2026-04-23) — Departamentos: rota destravada + exclusão em massa

- [x] **Bug fix — `/app/departamentos` não abria a tela real** (2026-04-23): `src/app/(app)/departamentos/page.tsx` estava como stub que chamava `router.replace("/app/areas")` — clicar em "Departamentos" no menu jogava o usuário em "Áreas". A tela real (`features/cadastros/departamentos/DepartamentosScreen.tsx` com CRUD contra `/api/departments`) existia, mas estava órfã sem page.tsx conectada. Corrigido para `<AuthGuard><DepartamentosScreen /></AuthGuard>` — padrão das demais rotas.
- [x] **Feature — Exclusão em massa de departamentos** (2026-04-23):
  - **Backend — contrato**: `DepartmentBulkDeleteRequest(Ids)` com `[MinLength(1), MaxLength(500)]` + `DepartmentBulkDeleteResponse(Requested, Deleted)` em `Contracts/Departments/DepartmentContracts.cs`.
  - **Backend — service**: `IDepartmentService.DeleteManyAsync(ids, ct) → int` com guardas (dedup via `Distinct`, ignora `Guid.Empty`, filtro tenant via query filter do `AppDbContext`). IDs inexistentes/de outro tenant são silenciosamente ignorados — a contagem retornada indica quantos foram efetivamente removidos.
  - **Backend — handler**: `IDeleteManyDepartmentsHandler` seguindo o padrão dos outros 5 handlers de Department.
  - **Backend — endpoint**: `POST /api/departments/bulk-delete` (usei POST em vez de DELETE com body — DELETE com body é mal-suportado em proxies e rewrites do Next dev). Valida `Count > 0` e `Count ≤ 500` (400 BadRequest se violado). Retorna `{ requested, deleted }`.
  - **Backend — DI**: `AddScoped<IDeleteManyDepartmentsHandler, DeleteManyDepartmentsHandler>()` no `Program.cs`.
  - **Testes**: 8 novos em `DepartmentServiceTests.cs` — `ComIdsValidos_RemoveTodos`, `ComListaVazia_RetornaZero`, `ComIdsInexistentes_RetornaZero`, `MisturaExistentesEInexistentes_RemoveApenasExistentes`, `IdsDuplicados_RemoveUmaVez` (dedup), `IgnoraGuidEmpty`, `NaoVaza_ParaOutroTenant` (dual-context pattern, reutiliza o factory `CriarServicoComOutroTenant` documentado na Sessão 31), `NaoAfetaDepartamentosNaoSelecionados` (garantia de que IDs não enviados não somem).
  - **Frontend — `DepartamentosScreen.tsx`**: adicionado state `selectedIds: Set<string>` + helpers `toggleOne` / `togglePage` / `clearSelection`. Nova coluna de checkbox nativo (`<input type="checkbox">` estilizado com Tailwind `accent-primary` — sem dependência nova, o UI kit não tem Checkbox). Header tem checkbox com `indeterminate` via ref (marca parcial quando só parte da página visível está selecionada). Linhas selecionadas ganham `bg-primary/5` + `data-state="selected"`. Barra contextual azul aparece quando há seleção: mostra `"N departamento(s) selecionado(s)"`, indicação de itens fora do filtro, botões "Limpar seleção" e "Excluir selecionados (N)". Dialog de confirmação separado (`bulkDeleteOpen`) com contagem explícita e estado `bulkDeleting` para desabilitar controles. Após sucesso, toast informa `X de Y` quando há discrepância (ex: IDs estale), limpa seleção e recarrega lista. Seleção é "efetiva" — IDs que sairam do filtro corrente são contabilizados separadamente mas não enviados no payload (evita apagar acidentalmente item fora do recorte visível).
- [x] **Regressão — 783/783 testes verdes** (2026-04-23): `dotnet build` 0 erros / 123 warnings pré-existentes; `dotnet test` **783 aprovados / 0 falhos** (775 Sessão 31 + 8 novos bulk delete). `npx tsc --noEmit` limpo; `npx eslint` limpo nos 2 arquivos tocados (`DepartamentosScreen.tsx` + `page.tsx`); HMR do Next dev entregou chunk novo com 9 referências a `selectedIds` + literal "Excluir selecionados" confirmado.

## Sessão 31 (2026-04-22) — Dashboard real por perfil (Gestor / RH / Diretor) — endpoint único + UI unificada

- [x] **Fecha lacuna histórica — Dashboard agregado por perfil** (2026-04-22): o `DashboardScreen.tsx` existente renderiza widgets livres (colcha de retalhos via `WidgetCatalog`), mas nunca houve uma visão oficial "por perfil". Sessão 31 entrega **3 perfis em 1 endpoint** (`GET /api/dashboard/agregado?perfil=gestor|rh|diretor`) — menos rotas, mais reuso de contrato, frontend consome com `perfil` na querystring.
  - **Backend — contrato `DashboardAgregadoContracts.cs`** (novo, `RHPortal.Api/RHPortal.Api/Contracts/Dashboard/`): 1 response envelope (`DashboardAgregadoResponse`) com 3 seções opcionais (`gestor` / `rh` / `diretor`) — só a do perfil solicitado vem preenchida; as outras ficam null. Três seções: `DashboardGestorSection` (8 KPIs + 2 listas: vagas mais antigas / candidaturas em destaque), `DashboardRhSection` (16 KPIs + 2 listas: vagas fora SLA / pré-admissões recentes), `DashboardDiretorSection` (11 KPIs + 2 listas: headcount por área / ciclos resumo). Enum string `PerfilAgregado = gestor|rh|diretor`.
  - **Backend — service `DashboardAgregadoService.cs`** (novo, `RHPortal.Api/RHPortal.Api/Application/Dashboard/`): facade `ObterAsync(perfil, funcionarioId, ct)` encaminha para `BuildGestorAsync` / `BuildRhAsync` / `BuildDiretorAsync`. Gestor resolve escopo por `Funcionario.GestorDiretoId == funcionarioId`; RH consulta agregados tenant-wide (vagas por status, pipeline por etapa, pré-admissões, admissões, notificações falhadas 7d, matching scores 48h); Diretor resolve headcount por `Area` + ciclos abertos/em calibragem + alçada salarial aprovada no mês. Reutiliza `SlaVagaMetaResolver.GetDiasMeta` para marcação `foraDoSla` em vagas. **Perfil sem dados ⇒ seção null** (não 403) — UX-safe: "gestor sem subordinados" vê dashboard vazio, não erro.
  - **Backend — endpoint `GET /api/dashboard/agregado`** (`DashboardController.cs`, método adicionado com `[HttpGet("agregado")]` + `[RequirePermission("dashboard.view")]`): query param `perfil` (gestor/rh/diretor) → 400 `BadRequest` se inválido; service injetado via `[FromServices]` para não impactar ctor do controller (que é legado). Responde `DashboardAgregadoResponse` JSON.
  - **Backend — DI** (`Program.cs`): `builder.Services.AddScoped<IDashboardAgregadoService, DashboardAgregadoService>()`.
  - **Testes** (`DashboardAgregadoServiceTests.cs`, **24/24 aprovados**): 4 facade (todos os perfis + perfil inválido), 4 gestor (KPIs básicos, diretos com dados incompletos, vagas paradas fora SLA, candidaturas em destaque filtradas por hierarquia), 5 RH (contagens vagas por status, pipeline por etapa macro, pré-admissões, admissões, notificações falhadas/matching scores 48h), 4 diretor (headcount por área, ciclos abertos/calibragem, alçada salarial mês, convites pendentes), 3 tenant isolation (gestor / RH / diretor — cada um via pattern dual-context `CriarServicoComOutroTenant` para driblar o override de `TenantId` no `SaveChangesAsync`), 4 edge cases (gestor sem diretos, RH sem dados, diretor sem áreas, facade sem `FuncionarioId` retorna seção vazia). Descobertos e documentados 2 gotchas do `AppDbContext.SaveChangesAsync`: (1) **sobrescreve `TenantId`** em entidades Added → solução: dual-context com 2 `ITenantContext` no mesmo InMemoryDatabase; (2) **sobrescreve `CreatedAtUtc`** em entidades Added → solução helper `AjustarCreatedAtAsync<T>` (save em Added → modificar property → save em Modified para backdate).
  - **Frontend — types `dashboardAgregadoTypes.ts`** (novo, `LioTecnica.Web.Next/src/features/dashboard/`): mirror 1:1 do contrato backend. `PerfilAgregado` union type + 3 sections + item types (vagas abertas gestor, candidaturas destaque, vagas fora SLA RH, pré-admissões recentes, headcount por área, ciclos resumo).
  - **Frontend — hook `useDashboardAgregado.ts`** (novo): consome `/api/dashboard/agregado?perfil=...` via `apiFetch` (tenant header auto-injetado). Estados `data / loading / error` + `refresh`. Trata 400 (perfil inválido), 401/403 (permissão), demais com HTTP status.
  - **Frontend — tela `DashboardVisaoPorPerfilScreen.tsx`** (novo, ~700 linhas): componente principal com 3 abas horizontais (Gestor/RH/Diretor), skeleton loader durante load, card de erro + botão Tentar novamente, `SecaoIndisponivel` quando seção vem null (mensagens diferenciadas por perfil: "gestor sem vínculo com funcionário", "RH sem módulo contratado", "diretor sem permissão"). Seções: **SecaoGestor** (5 cards "Meu time" + 4 cards "Carteira de vagas" + tabela vagas mais antigas com foreSla highlight + tabela candidaturas em destaque), **SecaoRh** (5 cards "Vagas" + 5 cards "Funil" + 6 cards "Pré-admissões & Admissões" + 2 cards "Saúde das automações" + tabela vagas fora SLA top + tabela pré-admissões recentes), **SecaoDiretor** (4 cards "Headcount" + 5 cards "Movimentação" + 3 cards "Ciclos e avaliações" + tabela headcount por área + tabela ciclos resumo). `KpiCard` reutilizável com tones (neutral/warning/success/danger), href opcional, loading state.
  - **Frontend — rota `/app/dashboard/visao-por-perfil/page.tsx`** (novo): shell minimalista envolvendo `DashboardVisaoPorPerfilScreen` com `AuthGuard`.
  - **Frontend — link no dashboard existente** (`DashboardScreen.tsx`): adicionado botão "Visão por perfil" na toolbar (`ChartBarStacked` icon) linkando para `/app/dashboard/visao-por-perfil`. Entrega aditiva — o dashboard livre com widgets permanece intocado.
- [x] **Regressão — 775/775 testes verdes** (2026-04-22): `dotnet build` 0 erros / 120 warnings pré-existentes; `dotnet test` 775 aprovados / 0 falhos (751 da Sessão 30 + 24 novos do DashboardAgregadoService). `npx tsc --noEmit` limpo; `npx next build` 126/126 páginas (inclui `/dashboard/visao-por-perfil` listado no output); `npx eslint` limpo nos arquivos tocados.
- [ ] **(follow-up)** Adicionar item `nav-dashboard-visao-perfil` no `NavegacaoManifest` apontando para `/dashboard/visao-por-perfil` (hoje a tela só é acessível via botão na toolbar do dashboard livre). Bucket natural seria `principais` ou `gestao-pessoas`, permissão `dashboard.view`. Mudança cirúrgica de 1 item + ajuste dinâmico no `NavegacaoSidebarServiceTests.Build_Wildcard_...`.
- [ ] **(follow-up)** Permitir scoping multi-perfil (ex: diretor que também é gestor de área técnica) via checkbox "mostrar também X" em vez de abas mutuamente exclusivas. Backend já suporta — frontend precisaria chamar endpoint 2× e mesclar, ou backend aceitar `perfil=gestor,diretor` (CSV).

## Sessão 30 (2026-04-22) — Desempenho sai da lista de lacunas: módulo ganha sidebar

- [x] **Fecha follow-up da Sessão 27 — UI do módulo `desempenho` exposta no `NavegacaoManifest`** (2026-04-22): backend de Desempenho existe desde as Sessões 21 e 22 (ciclos, autoavaliação, 360°, calibragem com comitê, 9-box, convites, export CSV) mas o módulo estava declarado no `ModuleCatalog` sem entradas de navegação — a sidebar simplesmente não o exibia. Três items novos no `NavegacaoManifest.cs` no bloco Gestão de Pessoas:
  - `nav-desempenho-minhas` → `/desempenho` ("Minhas Avaliações", ícone `trending-up`, permissão `desempenho.view`, ordem 100) — tela do colaborador (`DesempenhoMinhasAvaliacoesScreen.tsx` já existia em `features/feedback/desempenho/`).
  - `nav-desempenho-ciclos` → `/feedback/avaliacao` ("Ciclos de Avaliação", ícone `target`, permissão `desempenho.ciclos.manage`, ordem 110) — tela admin de RH (`CiclosAvaliacaoScreen.tsx` em `features/feedback/`, com criação/ativação/fechamento/convites/calibragem/export).
  - `nav-desempenho-ninebox` → `/feedback/nine-box` ("Nine Box", ícone `grid`, permissão `desempenho.calibragem.manage`, ordem 120) — matriz 3×3 com posicionamento + PDI automático (`NineBoxScreen.tsx` em `features/feedback/nine-box/`).
  - Bucket UI resolvido automaticamente para `gestao-pessoas` via `PackageKey` do módulo `desempenho`. Ícones (`trending-up`, `target`, `grid`) já estavam no `ICONS` map do `SidebarNavClient.tsx` — nada a adicionar no frontend.
  - As rotas Next existem há várias sessões vivendo sob `/feedback/*` por razão histórica (submódulo nasceu embutido no pacote Feedback). Migrar para `/desempenho/*` é cosmético e fica para uma próxima sessão — o módulo já resolve corretamente via `ModuleCatalog.ResolveModuleKey` (`PermissionKeyPrefixes: ["desempenho."]`).
- [x] **Testes — `ModuleScreensResolverTests` atualizado** (2026-04-22):
  - `ModulosComTelasNoManifesto_AparecemNoMapa`: lista `modulosObrigatorios` passa a incluir `"desempenho"` (antes era a única exceção documentada como "lacuna de produto"). Comentário do teste reescrito para refletir que o módulo não é mais exceção.
  - Novo `DesempenhoModule_IncluiMinhasAvaliacoesECiclosENineBox`: afirma que o resolver devolve exatamente as 3 telas de Desempenho, todas mapeadas para bucket `gestao-pessoas`, com permissão correta por tela (`desempenho.view` / `desempenho.ciclos.manage` / `desempenho.calibragem.manage`). Segue o padrão dos testes análogos de `RecrutamentoModule`, `CandidatosModule`, `FolhaPagamentoModule`.
- [x] **Regressão — 751/751 testes verdes** (2026-04-22): `dotnet build` 0 erros / 120 warnings pré-existentes (não relacionados); `dotnet test` 751 aprovados / 0 falhos / 0 ignorados (750 Sessão 29 + 1 teste novo de Desempenho). `tsc --noEmit` limpo (frontend não foi tocado — manifest é consumido dinamicamente via `GET /api/navegacao/sidebar`). Nenhum teste precisou ser adaptado por contagem — `NavegacaoSidebarServiceTests` já usa `NavegacaoManifest.Items.Count` dinâmico desde a Sessão 27.

## Sessão 29 (2026-04-22) — White-label real por tenant + roteiro de teste ponta-a-ponta

- [x] **Fecha follow-up da Sessão 26 — white-label real por tenant** (2026-04-22): novo bloco backend `TenantBranding` (entidade + migration idempotente + service + 2 controllers + DI + 17 testes) substitui as strings fixas da tela de login por configuração persistida por tenant.
  - **Entidade** `Domain/Entities/TenantBranding.cs` (`ITenantEntity`) com 7 campos visuais (`NomePortal`, `Subtitulo`, `RodapeTexto`, `CorPrimariaHex`, `CorSecundariaHex`, `LogoUrl`, `VersaoExibida`) + `UpdatedAtUtc`. Decisão arquitetural: **separar de `TenantConfiguracao`** (que cuida de regras de negócio/aprovações) para manter responsabilidades distintas.
  - **Migration idempotente** `AddTenantBrandingsTable` (reescrita manualmente para `CREATE TABLE IF NOT EXISTS` + índice único `IX_TenantBrandings_TenantId` em linha com `CLAUDE.md` para cenário multi-tenant).
  - **Service** `Application/TenantBranding/TenantBrandingService.cs` com `GetAsync`/`UpsertAsync`/`ResetAsync`/`GetPublicAsync`. Helpers puros `NormalizeText` (trim + clamp + whitespace→null) e `NormalizeColor` (valida `#RRGGBB`, uppercase; inválido→null em vez de lançar — UX-safe). Constantes de tamanho alinhadas com as colunas (`MaxNomePortal=80`, `MaxSubtitulo=160`, `MaxRodape=160`, `MaxLogoUrl=512`, `MaxVersao=32`).
  - **Admin controller** `TenantBrandingController` (`GET/PUT/DELETE /api/tenant-branding`) com gate `_userContext.IsAdmin` em mutations. **Public controller** `PublicBrandingController` (`GET /api/public/branding?tenant=<slug>`, `[AllowAnonymous]`, whitelistado no `TenantMiddleware.PublicPathsWithoutTenant`) usa `IServiceProvider.CreateScope` + `SetTenantId` manual para resolver branding pelo slug do tenant **sem autenticação**. **Sempre retorna 200** (DTO vazio se slug inválido/inexistente) — tela de login nunca quebra por typo.
  - **Frontend** (`LioTecnica.Web.Next`): nova lib `src/lib/tenant-branding.ts` (types + `BRANDING_DEFAULTS` + `applyBrandingDefaults` + `renderFooterText` com `{ano}` + `fetchPublicBranding` + helpers admin). `session.ts` ganhou `getLastTenantSlug`/`setLastTenantSlug`/`clearLastTenantSlug` (preservado pós-logout). `LoginScreen.tsx` resolve slug via `?tenant=` ou `localStorage.lastTenantSlug` e aplica overrides em logo/nome/subtítulo/gradient/botão/versão/rodapé (fallback defaults). Nova tela admin `TenantBrandingScreen.tsx` (form + **preview ao vivo** imitando o card real) com validação de hex client-side. Rota `/admin/tenant-branding` e sidebar entry `nav-admin-tenant-branding` (ícone `palette`, permissão `access.manage`, grupo `configuracoes`).
  - **Testes**: 17 em `TenantBrandingServiceTests.cs` (defaults vazios; upsert cria/atualiza/não duplica; string vazia→null; clamp; cor inválida→null; cor lowercase→UPPER; reset remove/idempotente; GetPublic; `NormalizeColor` Theory com 5 inputs válidos + 8 inválidos; `NormalizeText` empty/trim/clamp; **isolamento cross-tenant** via 2 AppDbContexts no mesmo InMemoryDb + `IgnoreQueryFilters`). `NormalizeText`/`NormalizeColor` expostos como `public static` para testabilidade direta (eram `internal`; sem `InternalsVisibleTo`). 27/27 resultados (17 métodos → 27 com Theory expansions).
  - **Regressão**: `dotnet build` 0 erros / 84 warnings pré-existentes; `dotnet test` **750/750 aprovados** (723 Sessão 28 → 750 = +27 branding). `npx tsc --noEmit` limpo, `npx eslint` limpo nos arquivos tocados.
- [x] **Fecha follow-up da Sessão 26 — `TESTE_FLUXO_ADMISSAO.md`** (2026-04-22): roteiro ponta-a-ponta de ~30 min na raiz do repo cobrindo 8 passos (seed dos 3 usuários → gestor solicita vaga → diretor aprova → RH publica → cadastra candidato + matching → processo seletivo → gestor aprova contratação → pré-admissão completa → integração TOTVS simulada). Inclui check-list de aceitação (13 itens), modo express (bash script com jq), limpeza idempotente, tabela de troubleshooting (7 sintomas comuns) e referência cruzada com os controllers. Integra com a white-label nova via `?tenant=<slug>` na URL de login. O endpoint `POST /api/dev/seed/usuarios-teste` continua apontando para esse arquivo via campo `proximoPasso` — agora o arquivo existe e resolve o gap.

## Sessão 28 (2026-04-22) — Encerramento dos épicos R&S em andamento (Onboarding por Cargo Macro + Notificações WhatsApp)

- [x] **Gap estrutural 1 — `SincronizarPreAdmisoesAtivasAsync` agora é acionado também nos saves por NivelCargo e por Cargo** (2026-04-22): `RHPortal.Api/Application/DocumentacaoPadrao/DocumentacaoPadraoService.cs` passa a chamar `SincronizarPreAdmisoesAtivasAsync(now, ct)` ao final de `SaveByNivelCargoAsync` e `SaveByCargoAsync` (antes só o save global disparava a propagação). Efeito: admin altera override por nível/cargo → pré-admissões ativas que hoje herdam daquele nível/cargo são recomputadas imediatamente (hierarquia `Cargo → NivelCargo → Global`), em vez de esperar o próximo save global ou reprocessamento manual. O método é idempotente e reconstrói a hierarquia por preadmissão, então pré-admissões de outros níveis não são afetadas. Fecha o sub-item (d) de "Pacote R&S — Onboarding por Cargo Macro (extras)".
- [x] **Gap estrutural 2 — Janela de silêncio agora cobre canal e-mail** (2026-04-22): `RHPortal.Api/Application/Candidaturas/CandidaturaNotificacaoService.EnviarEmailComLogAsync` passou a checar `EstaDentroSilencio(pref, now, out descricao)` antes de enfileirar. Quando a flag global `WhatsAppOptions.RespeitarSilencio` (nome histórico — a partir dessa revisão cobre ambos os canais) está ligada e o candidato está na janela configurada, registra log com status `NotificacaoStatus.IgnoradoSilencio` e não envia. O enum `IgnoradoSilencio` já era canal-agnóstico; a lógica original (Onda 10) bloqueava só WhatsApp — o candidato que configura "não quero ser notificado entre X e Y" tipicamente quer valer pro e-mail também (push no celular, marca de "não lido" etc.). Fecha o sub-item (d) de "Pacote R&S — Notificações WhatsApp (extras)".
- [x] **Verificação dos demais sub-itens do épico WhatsApp** (2026-04-22): auditoria do código revelou que (a) provedor real Twilio/Meta Cloud, (c) rate-limit/throttle por candidato e (e) seletor de idioma já estavam implementados em ondas anteriores não documentadas no backlog como fechadas. Marcados como concluídos aqui para alinhar documentação com realidade:
  - **(a)** `IWhatsAppMessageSender` + `LoggingWhatsAppMessageSender` (stub) + `TwilioWhatsAppMessageSender` + `MetaCloudWhatsAppMessageSender` com seletor via `WhatsApp:Provider` config (`Logging`/`Twilio`/`MetaCloud`).
  - **(c)** Rate-limit com janela deslizante + override por candidato via `CandidatoNotificacaoPreferencia.WhatsAppRateLimitMaxMensagens`, gerando log com status `IgnoradoRateLimit`.
  - **(e)** `CandidatoNotificacaoPreferencia.Idioma` (BCP-47) + `NotificacaoTemplate.Idioma` com fallback exato → idioma null → hardcoded.
- [x] **Testes — 5 novos em `DocumentacaoPadraoSincronizacaoTests.cs`** (2026-04-22): `SaveByNivelCargo_PropagaParaPreAdmisoesDoNivel`, `SaveByCargo_PropagaParaPreAdmisoesDoCargo`, `SaveByNivelCargo_NaoAfetaPreAdmisoesDeOutroNivel`, `SaveByCargo_RemocaoDeOverride_ReverteParaNivelOuGlobal`, `SaveByNivelCargo_RemocaoDeOverride_ReverteParaGlobal`. Comentário do teste legado `SaveGlobal_AtualizaObrigatorioDeDocumentoExistenteRespeitandoOverride` foi ajustado (removida a nota "a sync só é disparada por SaveAsync" — não é mais verdade).
- [x] **Testes — 4 novos em `CandidaturaNotificacaoRateLimitTests.cs`** (2026-04-22): `Sessao28_Silencio_Email_AtivoEDentroDaJanela_LogaIgnorado`, `Sessao28_Silencio_Email_ForaDaJanela_PermiteEnvio`, `Sessao28_Silencio_Email_DesabilitadoNaOptions_NaoBloqueia`, `Sessao28_Silencio_AtivoEmJanela_BloqueiaAmbosOsCanais`. Janelas de silêncio montadas em torno da hora local atual para garantir determinismo do teste.
- [x] **Regressão — 723/723 testes verdes** (2026-04-22): `dotnet build` 0 erros / 116 warnings pré-existentes; `dotnet test` 723 aprovados / 0 falhos / 0 ignorados (714 da Sessão 27 + 9 novos = 723). Sem alterações na UI — os 2 gaps fechados eram back-end puros e já tinham telas Admin correspondentes (editor por NivelCargo na Sessão 24 Onda 5, preferências do candidato no portal autenticado).

## Sessão 27 (2026-04-20) — Owner vê telas por módulo (Trilha A) + Folha oculta enquanto pacote inativo (Trilha B)

- [x] **Trilha A — Owner vê telas no detalhe por módulo** (2026-04-20): contratos novos `ModuleScreenResponse` e `TenantModuleDetailedResponse` em `Contracts/Modules/ModuleContracts.cs`; novo `Application/Navegacao/ModuleScreensResolver.cs` (estático, pré-computa `moduleKey → telas` a partir do `NavegacaoManifest.Items` reutilizando `ModuloKeyOverride ?? ModuleCatalog.ResolveModuleKey` e `NavegacaoManifest.ResolveGrupoUi`); novo `TenantModuleService.ListDetailedAsync(tenantId, ct)`; novo endpoint `GET /api/owner/tenants/{id}/modules/detailed` em `OwnerController` (padrão `CreateScope()` + `SetTenantId`); `TabModulos.tsx` ganhou expand/collapse por card exibindo tabela `Tela / Href / Bucket UI / Ordem` e badge com contador de telas por módulo e por pacote. Resolver é **puramente estrutural** — não gateia `Package.IsActive`, pra separar claramente "o que o módulo libera" (catálogo) de "o que o usuário enxerga" (runtime da sidebar).
- [x] **Trilha B — Folha de Pagamento oculta enquanto pacote inativo** (2026-04-20): `NavegacaoSidebarService` mudou o gate `package is null || !package.IsActive` de "emite bloqueado com cadeado" para `continue` direto no loop. Diretiva explícita do usuário: *"não mostrar no menu com cadeado, somente depois de implementarmos"*. Reversível em 1 linha — basta setar `PackageCatalog.FolhaPagamento.IsActive = true` quando o produto existir e os 3 itens (batida-ponto / comissões / desligamentos) voltam a aparecer (com gate normal de módulo/tenant). O Owner **continua vendo as 3 telas** no card do módulo (via Trilha A) — isso é intencional: a visão do Owner é estrutural e deve refletir o catálogo completo.
- [x] **Testes** (2026-04-20): novo `ModuleScreensResolverTests.cs` com 10 testes (cobertura total do manifesto, whitelist de módulos obrigatórios, casos específicos de R&S, consistência com `NavegacaoSidebarService`, defensivos); +3 testes em `TenantModuleServiceTests.cs` (`ListDetailed_*`); 2 renomeações + 1 cálculo dinâmico em `NavegacaoSidebarServiceTests.cs` (agora subtrai itens de pacotes inativos em vez de contagem fixa). **714/714 verde** (699 Sessão 26 → 714); `tsc` e `eslint` limpos no arquivo tocado.
- [x] **(follow-up)** `desempenho` declarado no `ModuleCatalog` mas sem telas no `NavegacaoManifest` — implementar UI do módulo para sair da lista de exceções do teste `ModulosComTelasNoManifesto_AparecemNoMapa` (hoje documentado como lacuna de produto). **Fechado na Sessão 30 (2026-04-22)** — 3 items novos no manifesto (Minhas Avaliações, Ciclos, Nine Box) apontando para as rotas Next que já existiam em `/desempenho` e `/feedback/*`; teste saiu da whitelist de exceções + teste novo `DesempenhoModule_IncluiMinhasAvaliacoesECiclosENineBox`.
- [ ] **(follow-up)** Reativar Folha de Pagamento quando o produto existir: setar `PackageCatalog.FolhaPagamento.IsActive = true` — os 3 itens voltam automaticamente (com gate normal), os testes renomeados precisarão ser adaptados (comportamento volta a ser "emite com gate") e o toggle do Owner volta a funcionar. Checklist a manter em mente quando a fase nascer.

## Sessão 26 (2026-04-20) — Tenant seed consertado + usuários de teste + LoginScreen white-label

- [x] **Bug fix — `TenantProvisioningService.SeedTenantAsync` não criava roles padrão** (2026-04-20): `SeedTenantAsync` (endpoint owner `POST /api/owner/tenants/{id}/seed`) e `RunSeedAsync` (provisionamento de novo tenant) agora chamam `MenuRoleSeeder.EnsureRolesExistAsync` após `AdminAccessSeeder.EnsureAsync`. Antes, as 3 roles padrão (`Operacional`, `Gestor`, `Recrutador`) só existiam em tenants já presentes no master no momento do startup da API (via `DbSeeder.MigrateAndSeedAsync`). Qualquer tenant criado depois ou re-seedado ficava sem essas roles até o próximo restart. `MenuRoleSeeder.EnsureRolesExistAsync` promovido de `private` para `public`.
- [x] **Bug fix — `InboxFolder.RootPath` Windows hardcoded quebrando build no macOS** (2026-04-20): `appsettings.Development.json` tinha `"RootPath": "C:\\Users\\davio\\..."`. O `InboxFolderWatcherService` fazia `Directory.CreateDirectory` dessa string e, em macOS/Linux, criava um diretório literal dentro do projeto com backslashes no nome. Esse diretório quebrava o globbing recursivo do MSBuild (`**/*.resx`, `**/*.razor`, `**/*.cs`) com erro `MSB3552: O arquivo de recurso "**/*.resx" não foi encontrado`. Corrigido para `App_Data/Inbox` (cross-platform, App_Data já existe no projeto).
- [x] **Bug fix — `DevSeedController.Seed` falhava com `DateTime.Kind=Unspecified`** (2026-04-20): `pessoa1.DataNascimento` e `pessoa2.DataNascimento` agora usam `DateTime.SpecifyKind(new DateTime(...), DateTimeKind.Utc)`. A coluna Postgres é `timestamp with time zone` e o Npgsql rejeita `Kind=Unspecified`.
- [x] **`DevSeedController` liberado em Development** (2026-04-20): `[Authorize]` → `[AllowAnonymous]` + implementa `IActionFilter` com guard central `OnActionExecuting` que devolve `NotFound()` quando `IHostEnvironment.IsDevelopment() == false`. Em Development basta `X-Tenant-Id` no header; em homologation/production retorna 404. Motivação: seed endpoints não podem depender de JWT quando o tenant acabou de ser criado e ainda não há usuário autenticável.
- [x] **Usuários de teste criados no tenant `liotecnica`** (2026-04-20): `POST /api/dev/seed/usuarios-teste` retornou 3 usuários com senha `YkmF@2022*`:
  - `gestor@teste.local` (Carlos Gestor, roles `Gestor`+`Admin`, subordinado de Roberto)
  - `diretor@teste.local` (Roberto Diretor, role `Admin`, gestor dos outros dois)
  - `rh@teste.local` (Ana RH, roles `Admin`+`Recrutador`, subordinada de Roberto)
- [x] **Dataset de teste populado** (2026-04-20): `POST /api/dev/seed` criou vaga `SEED-DEV-001` (Dev Backend Senior .NET) + 5 candidatos em Status distintos + 2 talentos no banco + projeto "Rodada 1" com 4 fases + 4 candidatos posicionados (Ana na Proposta pronta pra aprovar). `POST /api/dev/seed/pre-admissoes` criou 5 mocks TOTVS com status `Aprovada` para testar `GET /api/pre-admissao/integracao/pendentes`.
- [x] **Smoke test de login** (2026-04-20): 3/3 logins dos novos usuários via `POST /api/auth/auto-login` retornaram HTTP 200 com JWT válido e claim `tenant:"liotecnica"`.
- [x] **LoginScreen white-label** (2026-04-20, `LioTecnica.Web.Next/src/features/auth/LoginScreen.tsx`): removido bloco "Bem-vindo ao" + `<RenderRHLogo />` + `<h1>RENDER</h1>`; substituído por ícone `<Building2>` em container com gradiente `#0C3A64 → #105291` + "Portal de RH" (text-2xl, slate-800) + subtítulo "Gestão de pessoas e recrutamento". Footer "© 2026 QUALIIT SOLUÇÕES EM TECNOLOGIA" → "© 2026 · Portal de RH". Link LGPD preservado. Import `RenderRHLogo` retirado (componente continua no projeto, só não é mais usado na tela de login). `npx tsc --noEmit` 0 erros.
- [x] **(follow-up)** tornar "Portal de RH" / versão do footer configuráveis por tenant (via `env` ou config backend) para white-label real por empresa — **fechado na Sessão 29 (2026-04-22)** via config backend (entidade `TenantBranding` + migration idempotente + 2 controllers + tela admin com preview ao vivo)
- [x] **(follow-up)** criar/atualizar `TESTE_FLUXO_ADMISSAO.md` com roteiro ponta-a-ponta usando os 3 perfis (gestor solicita → diretor aprova → RH preenche vaga e candidatos → processo seletivo → aprovação do gestor → pré-admissão → integração TOTVS) — **fechado na Sessão 29 (2026-04-22)** com roteiro de 8 seções na raiz do repo (pré-requisitos → seed 3 usuários → fluxo 8 passos → checklist 13 itens → modo express → cleanup → troubleshooting → cross-ref)

## Sessão 24 (2026-04-20) — Navegação unificada (fases A + B + C concluídas)

- [x] **Fase A — Backend: manifesto code-first + endpoint `GET /api/navegacao/sidebar`** (2026-04-20): `NavegacaoManifest` (8 buckets fixos + todos os `NavManifestItem` com `GrupoUiOverride`), `NavegacaoSidebarService` compondo permissões × módulos × tenant, `NavegacaoController`. Inclui regressão para pacote inativo (cadeado em vez de invisível) e owner root via `contextoEspecial="owner-root"`. **+3 permissões Folha** no `RolePermissionManifest.TenantPermissions` (`folha.batida-ponto.view`, `folha.pagamento-extra.view`, `folha.desligamentos.view`) pra itens travados continuarem visíveis com cadeado.
- [x] **Fase B — Frontend: sidebar/topbar/guard como puros renderers** (2026-04-20): `NavegacaoSidebarProvider` + schema Zod + `navegacao.ts`; `SidebarNavClient.tsx` caiu de ~1028 pra ~350 linhas (removido `LOCKED_NAV_HREFS`, `HIDDEN_ROUTES`, `RECRUTAMENTO_ROUTES`, `GESTAO_PESSOAS_ROUTES`, `CADASTROS_*_ROUTES`, `FEEDBACK_ROUTES`, `getModuleKey`, `ROUTE_MAP`, `buildRecruitmentSidebar`); `RouteAllowlistGuard` lê `visibleHrefs` do provider; `permissionManifest.ts` deletado. `tsc`/`eslint` limpos.
- [x] **Fase C — Alinhar buckets por `PackageKey`/core** (2026-04-20): overrides declarativos para casos pontuais (`eixo-vaga` → cadastros, `api-keys`/`tenant-config` → configuracoes), Folha aparece travada com `motivoBloqueio="pacote-inativo"`, +4 testes de bucket em `NavegacaoSidebarServiceTests`. **589/589 testes verdes** (585 + 4). Drift eliminado — taxonomia única, code-first, reversível.
- [x] **Onda 2 — Auditoria de Notificações de Candidatura** (2026-04-20): fecha o sub-item (f) de "Pacote R&S — Notificações WhatsApp (extras)". Backend: `CandidaturaNotificacaoService.ListarLogsAsync` com filtros (canal/status/etapa/candidato/candidatura/intervalo de data), paginação (page≥1, pageSize clampado em [1,200]), ordenação `CriadoEmUtc DESC`, e enriquecimento de candidato/vaga via subselects. Novo `NotificacoesCandidaturaController` com `[RequireModule("recrutamento")]` + `[RequirePermission("audit.view")]`. Novos contratos `NotificacaoCandidaturaLogItem`/`NotificacaoCandidaturaLogsResponse`. Frontend: types + client `listarNotificacoesCandidaturaLogs` no `candidaturaApi.ts`; tela `NotificacoesCandidaturaLogsScreen.tsx` com filtros, tabela expansível, badges de resumo (enviados/falhados/ignorados) e paginação; rota `/administracao/notificacoes-candidatura`. Manifesto: `nav-admin-notif-candidatura` (permissão `audit.view`). **599/599 testes verdes** (589 + 10 novos em `CandidaturaNotificacaoServiceTests`). Sub-itens (a/b/c/d/e) continuam abertos — ver entrada de WhatsApp extras abaixo.
- [x] **Onda 3 — `Candidato.VagaId` resgatado como cache da candidatura ativa** (2026-04-20): fecha o sub-item (c) de "Portal externo autenticado (extras)" com uma mudança arquitetural — em vez de refatorar 67 warnings `CS0618` em 12 arquivos (Reports, Dashboard, Inbox etc.), `Candidato.VagaId` foi redefinido como cache O(1) da candidatura ativa mais recente (fonte-de-verdade continua em `Candidaturas`). `[Obsolete]` removido. Novo método `CandidaturaService.RecalcularVagaPrincipalAsync(candidatoId, ct)` chamado após `GetOrCreateAsync` e `AvancarEtapaAsync` (preferência ativa > encerrada > preservar atual se nenhuma). `CandidatoService` opcionalmente injeta `ICandidaturaService` e chama `EnsureCandidaturaAndSyncAsync` em 3 pontos (create-by-email, create-new, update-vaga). Migration idempotente `SyncCandidatoVagaIdFromCandidaturas` com PostgreSQL `DISTINCT ON` (2 passes: ativa mais recente → fallback qualquer mais recente). Build limpo (0 warnings CS0618). **607/607 testes verdes** (599 + 7 novos em `CandidaturaServiceTests`, cobrindo sync na criação, múltiplas candidaturas, encerramento, idempotência e preferência ativa>encerrada).
- [x] **Onda 4 — Editor de templates de notificação por (etapa × canal)** (2026-04-20): fecha o sub-item (b) de "Pacote R&S — Notificações WhatsApp (extras)". Backend: nova entidade `NotificacaoTemplate` (tenant + 7 etapas × 2 canais + Assunto opcional + Corpo até 4000 chars + auditoria) com índice único `(TenantId, Etapa, Canal)` e query filter por tenant. `INotificacaoTemplateService`/`NotificacaoTemplateService` com `GetDefault` / `GetEfetivoAsync` / `ListarMatrizAsync` / `SaveAsync` (upsert; salvar igual ao default remove override) / `RestoreDefaultAsync` (idempotente) + helper estático `ResolverPlaceholders`. `CandidaturaNotificacaoService` passa a resolver templates via serviço (fallback hardcoded para testes legados). Novos contratos `NotificacaoTemplateItem` / `NotificacaoTemplateSaveRequest`. Novo `NotificacoesTemplatesController` com `GET` (matriz 14 linhas), `POST` (upsert) e `DELETE /{etapa}/{canal}` (restore default), todos com `[RequireModule("recrutamento")]` + `[RequirePermission("audit.view")]`. Migration idempotente `AddNotificacoesTemplatesTable` (`CREATE TABLE IF NOT EXISTS` + índice único). Frontend: types + 3 funções no `candidaturaApi.ts`; tela `NotificacoesTemplatesScreen.tsx` com grid 7 etapas × 2 canais, edição inline de assunto/corpo, badge "Padrão"/"Customizado", botão Salvar + Restaurar padrão por célula, placeholders documentados; rota `/administracao/notificacoes-templates`. Manifesto: `nav-admin-notif-templates` (permissão `audit.view`, ordem 145). **621/621 testes verdes** (607 + 14 novos em `NotificacaoTemplateServiceTests` cobrindo matriz default, upsert, reversão ao padrão, canal WhatsApp ignorando assunto, validação de corpo vazio/>4000, placeholders com null).
- [x] **Onda 5 — UI admin docs padrão por NivelCargo** (2026-04-20): fecha o sub-item (a) de "Pacote R&S — Onboarding por Cargo Macro (extras)". Extensão da `DocumentacaoPadraoScreen.tsx` com sistema de abas ("Padrão Global" / "Por Nível de Cargo") — nenhuma quebra de UI existente. Integra `/api/nivel-cargo/lookup` para o select de níveis; consome `GET/PUT /api/admin/documentacao-padrao/por-nivel-cargo/{nivelCargoId}` pra overrides. Cada row tem seletor + badge "Custom" (violet) quando `overrideAtivo=true`, botão "Herdar" individual (restaura do global + desliga overrideAtivo), botão "Limpar todos" (remove o override inteiro) e "Salvar overrides" (envia SOMENTE itens com `overrideAtivo=true`; ausentes voltam a herdar). Banner instrucional quando sem nível selecionado. tsc/eslint limpos.
- [x] **Onda 6 — Histórico de alterações da documentação padrão** (2026-04-20): fecha o sub-item (c) de "Pacote R&S — Onboarding por Cargo Macro (extras)". Backend: nova entidade `DocumentacaoPadraoHistorico : ITenantEntity` (`Escopo` Global/PorNivelCargo, `NivelCargoId?`, `TipoDocumento`, `ConfiguracaoAnterior?`/`Nova?` short, `Acao` Criado/Alterado/Removido, `UserId?`/`UserNome?` congelado, `CriadoEmUtc`). Enums auxiliares `DocumentacaoPadraoEscopo` e `DocumentacaoPadraoAcao`. Índices `(TenantId, CriadoEmUtc)` e `(TenantId, Escopo, NivelCargoId)` + FK NivelCargo com `OnDelete.SetNull` (preserva histórico mesmo após exclusão de nível). `DocumentacaoPadraoService` ganha ctor overload com `ICurrentUserContext` (o original sem `ICurrentUserContext` continua pra compat com testes legados), `SaveAsync` e `SaveByNivelCargoAsync` gravam entradas com diff antes→depois, novo método `ListarHistoricoAsync(escopo?, nivelCargoId?, page, pageSize)` com paginação clampada (`pageSize∈[1,200]`) usando materialização intermediária (anonymous type) para evitar EF não traduzir `LabelPorTipo` static. Novos contratos `DocumentacaoPadraoHistoricoItem` e `DocumentacaoPadraoHistoricoResponse`. `DocumentacaoPadraoController.Historico` em `GET /api/admin/documentacao-padrao/historico`. Migration idempotente `AddDocumentacaoPadraoHistoricosTable` (`CREATE TABLE IF NOT EXISTS` + 3 `CREATE INDEX IF NOT EXISTS`). Frontend: terceira aba "Histórico" na `DocumentacaoPadraoScreen.tsx` com filtros (escopo, nível cargo condicional), tabela de 7 colunas (Quando / Escopo / Nível / Documento / Ação com badge emerald/amber/slate / Antes→Depois / Usuário) e paginação cliente. **633/633 testes verdes** (621 + 12 novos em `DocumentacaoPadraoHistoricoTests` cobrindo Criado/Alterado/Removido para ambos os escopos, no-op quando valor não muda, filtro por escopo, filtro por nivelCargoId, paginação/clamp, ordenação desc, label do tipo e nome do nível).

## Pendentes

### Épicos estratégicos (derivados da visão arquitetural — ver `knowledge-base/visao-arquitetural.md` §6.2)

- [x] **Camada de entitlement em dois níveis** (épico fechado em 2026-04-17): backend + UI Owner agrupada por pacote + testes dedicados (447/447)
- [x] **UI Owner — gestão de pacotes**: `TabModulos.tsx` reescrito com seção "Pacotes contratados" (card com toggle master + grid de módulos filhos), "Opcionais avulsos" e "Core". Regra EFETIVA (AND) refletida visualmente (2026-04-17)
- [x] **Cobertura de testes — entitlement**: `TenantPackageServiceTests` (17 cenários) + 5 cenários de composição em `TenantModuleServiceTests`. Inclui proteção contra pacote `IsActive=false` vazar pro set efetivo (2026-04-17)
- [x] **Pacote R&S — Carteira de vaga** (2026-04-17): `Vaga.RecrutadorResponsavelUserId` já existia + `VagasDataScope` com 3 valores (All/ByArea/ByRecrutador) + filtro canônico em `VagaService.ApplyVagasDataScopeFilter`. Agora extendido com **`VagasDataScope.ByGestorRecrutador`** (filtro via `RecrutadorResponsavelUser.Funcionario.GestorDiretoId == currentUser.FuncionarioId`), novo option no `AdminRoleFormModal.tsx`. Kanban view já existia em `VagasScreen.tsx` (drag-drop por status). Testes dedicados `VagaCarteiraScopeTests` (10 cenários, 457/457 verdes)
- [x] **Pacote R&S — Eixo de vaga e SLA por eixo** (2026-04-17): `EixoVaga` criado em `RHPortal.Api/Domain/Entities/EixoVaga.cs` (tenant + Code único + SLA opcional); tabela `EixosVaga` + `Vagas.EixoVagaId` via migration idempotente `AddDescricoesCargoEEixosVaga`. `EixoVagaController` (CRUD + lookup + guard contra delete em uso). `VagaResponse` expõe `EixoVagaId/Code/Name/SlaDiasMetaFechamento` + campo derivado `SlaEfetivoDias` (fallback eixo → vaga). FE `EixoVagaCadastroScreen.tsx` + rota `/eixo-vaga` + sidebar (`nav-eixo-vaga`, permissão `vagas.view`, classificado em Cadastros Operacionais). Build 457/457 verdes + tsc clean
- [x] **Pacote R&S — Travar faixa salarial** (2026-04-17): `Vaga.TravarFaixaSalarial` + 4 campos de alçada (`AlcadaSalarialAprovadaPorUserId`, `AlcadaSalarialAprovadaEmUtc`, `AlcadaSalarialJustificativa`, `AlcadaSalarialObservacaoAprovador`) via migration idempotente `AddAlcadaSalarialToVaga`. `VagaService.ValidateFaixaSalarialAsync` lê `FaixaSalarial` por `JobPositionId` (última atualizada) e lança `InvalidOperationException` quando salário proposto fora da faixa e sem alçada aprovada. Novos endpoints `POST /api/vagas/{id}/aprovar-alcada-salarial` e `POST /api/vagas/{id}/limpar-alcada-salarial` (guard Admin/Owner/RH). `VagaResponse` expõe faixa + violação + campos de alçada. Frontend: toggle "Travar faixa salarial" na aba Remuneração do `VagaFormModal`. 465/465 testes verdes (+8 novos em `VagaFaixaSalarialTests`)
- [x] **Pacote R&S — Aceite digital da proposta** (2026-04-17): nova entidade `PropostaVaga` (Rascunho/Enviada/Visualizada/Aceita/Recusada/Expirada/Cancelada) + migration idempotente `AddPropostasVaga` (indexes em tenant+vaga/candidato/token; FK Restrict). `PropostaVagaService` implementa state machine completo, geração de `AccessToken` hex 64 via `RandomNumberGenerator`, expiração default 7 dias e auto-expiração. Endpoints autenticados `/api/propostas-vaga` (CRUD + `/enviar` + `/cancelar`, guard Admin/Owner/RH) e públicos `/api/public/propostas/{token}` (GET marca Visualizada; POST `/aceitar` e `/recusar` capturam nome, IP, UA como evidência). Frontend: tela admin `/recrutamento/propostas-vaga` com listagem + modal de criação + "Copiar link" + cancelar; página pública `/PortalVagas/Proposta/[token]?tenantId=...` com aceite/recusa. 480/480 testes verdes (+15 em `PropostaVagaServiceTests`). Bonus: diagnosticado e removido diretório malformado `C:\Projetos\RHPortal\Inbox` que bloqueava MSBuild wildcard expansion.
- [x] **Pacote R&S — Portal externo autenticado (MVP)** (2026-04-17): nova entidade `Candidatura` (junction Candidato↔Vaga com histórico de etapa macro) + `CandidaturaEtapaHistorico` + enum `EtapaMacroCandidatura` (Aplicada/EmTriagem/Entrevista/Teste/Proposta/Contratado/Recusado/Desistiu) e `CandidaturaStatus` (Ativa/Contratado/Reprovado/Desistiu/Arquivada). Migration idempotente `AddCandidaturasJunction` com backfill dos `Candidato.VagaId` existentes. `CandidaturaService` (get-or-create idempotente, listagem por candidato com histórico, transição de etapa com guard de encerrado). `PublicCandidaturasController.Create` agora registra uma `Candidatura` a cada submissão (best-effort). Novo endpoint `GET /api/public/portal-auth/minhas-candidaturas/{candidatoId}`. Frontend: `MinhasCandidaturasSection` consome a API real quando há sessão de candidato (fallback para localStorage). 491/491 testes verdes (+11 em `CandidaturaServiceTests`)
- [x] **Pacote R&S — Portal externo autenticado (extras)** (2026-04-20, Sessão 23): (a) **✓** kanban admin em `/recrutamento/candidaturas` (8 colunas por `EtapaMacroCandidatura` + drag & drop que chama `AvancarEtapaAsync` com observação); (b) **✓** `PropostaVaga.CandidaturaId` via migration idempotente `AddCandidaturaIdToPropostaVaga` + backfill automático das propostas antigas + `PropostaVagaService.CreateAsync` chamando `CandidaturaService.GetOrCreateAsync` (novas propostas sempre amarradas); (c) **✓** `Candidato.VagaId` marcado `[Obsolete]` com plano de migração (7 call-sites em `CandidatoService` ficaram como warning ativo — checklist de refatoração); (d) **✓** notificação por etapa já vinha da Fase 3E (`CandidaturaNotificacaoService` dispara e-mail + WhatsApp best-effort em cada `AvancarEtapaAsync`). 560/560 testes verdes.
- [x] **Pacote R&S — Onboarding por Cargo Macro** (2026-04-17): nova entidade `DocumentacaoPadraoPorNivelCargoConfig` (override por `NivelCargo` sobre o `DocumentacaoPadraoConfig` global). Migration idempotente `AddDocumentacaoPadraoPorNivelCargo` com FK→`NiveisCargo` (Cascade) e índice único `(TenantId, NivelCargoId, TipoDocumento)`. `DocumentacaoPadraoService` extendido com `GetByNivelCargoAsync` (merge override + fallback global, com flag `OverrideAtivo` por item), `SaveByNivelCargoAsync` (upsert + remoção de overrides ausentes no payload → voltam a herdar) e `GetTiposEfetivosAsync`. Novos endpoints `GET/PUT /api/admin/documentacao-padrao/por-nivel-cargo/{nivelCargoId}` (guard Admin/Owner). `PreAdmissaoService.IniciarManualAsync` agora resolve `JobPosition.NivelCargoId` e mescla overrides ao seed de `PreAdmissaoDocumentoSolicitado`. 504/504 testes verdes (+13 em `DocumentacaoPadraoPorNivelCargoTests`).
- [x] **Pacote R&S — Onboarding por Cargo Macro (extras)** (épico fechado em 2026-04-22, Sessão 28): **(a) ✓ UI admin para editar templates por NivelCargo** (fechado em 2026-04-20, Sessão 24 Onda 5 — aba dedicada + overrides com herança); **(b) ✓ override por `Cargo` específico** (fechado em ondas anteriores — `DocumentacaoPadraoPorCargoConfig` + `DocumentacaoPadraoPorCargoTests` com 9 cenários já cobrindo hierarquia cargo→nível→global); **(c) ✓ histórico de alteração do template com `UserId`** (fechado em 2026-04-20, Sessão 24 Onda 6 — entidade + endpoint + UI + 12 testes); **(d) ✓ `SincronizarPreAdmisoesAtivasAsync` respeita overrides por NivelCargo e por Cargo** (fechado em 2026-04-22, Sessão 28 — propagação plugada nos saves por nível e por cargo, com 5 testes novos em `DocumentacaoPadraoSincronizacaoTests`).
- [x] **Pacote R&S — Notificações WhatsApp** (2026-04-17): orquestrador `CandidaturaNotificacaoService` dispara e-mail (`IEmailQueueService.EnqueueRawAsync`) e WhatsApp (`IWhatsAppMessageSender`) a cada transição de `EtapaMacroCandidatura`. Nova entidade `NotificacaoCandidaturaLog` (tenant, canal, status, destino, mensagem, erro) + migration idempotente `AddNotificacaoCandidaturaLog` (indexes em `(TenantId, CandidaturaId)` e `(TenantId, CandidatoId)`). Abstração plugável `IWhatsAppMessageSender` + stub `LoggingWhatsAppMessageSender` (logs) para trocar pelo provedor real (Twilio / Cloud API / Ítalo) apenas no DI. Respeita `CandidatoNotificacaoPreferencia` (default email=opt-in / whatsapp=opt-out enquanto sem provedor). Normalização E.164 com fallback Brasil (+55). Integração best-effort em `CandidaturaService.AvancarEtapaAsync` (catch + log, não quebra a transição). Templates por etapa (EmTriagem/Entrevista/Teste/Proposta/Contratado/Recusado/Desistiu + default). 518/518 testes verdes (+14 em `CandidaturaNotificacaoServiceTests`).
- [x] **Pacote R&S — Notificações WhatsApp (extras)** (épico fechado em 2026-04-22, Sessão 28): **(a) ✓ provedor real** (Twilio + Meta Cloud API prontos, seletor via `WhatsApp:Provider` config entre `Logging`/`Twilio`/`MetaCloud`), **(b) ✓ UI admin de templates por etapa** (fechado em 2026-04-20, Sessão 24 Onda 4 — `NotificacoesTemplatesScreen` com editor 7×2 + backend completo + persistência por tenant + helper estático para resolução de placeholders), **(c) ✓ rate limit / throttle por candidato** (janela deslizante configurável + override por candidato via `CandidatoNotificacaoPreferencia.WhatsAppRateLimitMaxMensagens`, status `IgnoradoRateLimit`), **(d) ✓ respeitar `SilencioInicio/Fim` da `CandidatoNotificacaoPreferencia`** (fechado em 2026-04-22, Sessão 28 — aplicado também ao canal e-mail; 4 testes novos em `CandidaturaNotificacaoRateLimitTests`), **(e) ✓ seletor de idioma** (`Idioma` na preferência + `NotificacaoTemplate.Idioma` + fallback exato→null→hardcoded), **(f) ✓ tela admin para auditar `NotificacoesCandidaturaLogs`** (fechado em 2026-04-20, Sessão 24 Onda 2 — ver entrada acima).
- [x] **Pacote Gestão de Pessoas — Módulo Desempenho (MVP)** (2026-04-17): módulo `desempenho` registrado no `ModuleCatalog` sob pacote `gestao-pessoas` + permissões `desempenho.view` / `desempenho.ciclos.manage` no `RolePermissionManifest`. `AvaliacaoCiclo` extendido com `Descricao`, `DataInicio/Fim` e novo status `Rascunho=2` (append-safe). `AvaliacaoService.CriarCicloAsync` aceita `IniciarEmRascunho` e valida `DataFim ≥ DataInicio`; novo `AtivarCicloAsync` (Rascunho→Aberto, idempotente; Fechado lança). `ResponderAsync` recusa Rascunho. `AvaliacaoController` decorado com `[RequirePermission]` + endpoint `POST /ciclos/{id}/ativar`. Migration idempotente `AddAvaliacaoCicloLifecycleFields`. 534/534 testes verdes (+17 novos em `AvaliacaoServiceTests`).
- [x] **Pacote Gestão de Pessoas — Módulo Desempenho (completo)** (2026-04-20): (a) UI admin completa (ativar/fechar ciclo, gerar convites, calibragem com comitê, decidir versão final, download CSV, criar em rascunho) em `CiclosAvaliacaoScreen.tsx`; (b) `NineBoxAssessment.CicloAvaliacaoId` + geração automática ao decidir calibragem (`gerarNineBox=true`); (c) `AvaliacaoCalibragem` com fluxo Pendente → Calibrado (comitê) → Decidido (gestor = palavra final), rastreando versões Gestor e Comitê paralelamente; (d) `AvaliacaoConviteService` gera convocações por hierarquia (`Funcionario.GestorDiretoId`) com tipos Autoavaliacao/GestorParaDireto/DiretoParaGestor/Par, idempotente, e envia e-mail via `IEmailQueueService` agrupado por avaliador; (e) export CSV em `AvaliacaoService.ExportarResultadosCsvAsync` (BOM UTF-8, colunas Gestor + Comitê + status). Novo enum `AvaliacaoCicloStatus.EmCalibragem=3` para ciclos em workflow de comitê. Permissões novas: `desempenho.convites.manage`, `desempenho.calibragem.manage`, `desempenho.calibragem.decidir`, `desempenho.export`. Migration idempotente `AddAvaliacaoConvocacoesECalibragem`. 552/552 testes verdes (+18 novos: 8 ConviteService + 10 CalibragemService).
- [x] **Pacote Gestão de Pessoas — Remover Gamificação** (2026-04-17 — removida do `permissionManifest.ts`; telas/rotas intactas pra reversibilidade)
- [x] **Pacote Folha de Pagamento — Ocultar itens transitórios** (2026-04-17 — `Batida de Ponto`, `Pagamento Extra`, `Desligamentos` movidos para `HIDDEN_ROUTES`; reversível quando o pacote oficial nascer)
- [x] **Core Cadastros — Hierarquia de Centros de Custo** (2026-04-17): `CentroCusto.ParentId` + migration idempotente `AddParentIdToCentroCusto`, endpoint `GET /api/centros-custo/tree` (árvore recursiva), validação anti-ciclo no PUT, `DELETE` bloqueado se houver filhos, UI com select de pai + coluna Pai. 447/447 verdes
- [x] **Core Cadastros — Descrição de Cargos** (2026-04-17): entidade nova `DescricaoCargo` (code, title, summary, responsibilities, requirements, niceToHave, benefits, isTemplate, NivelCargoId opcional), migration idempotente `AddDescricoesCargo`, `DescricaoCargoController` com CRUD + lookup, rota `/descricao-cargo` no sidebar (permissão `jobpositions.view`), tela com editor multi-seção e filtro Template/Uso direto. 447/447 verdes
- [x] **Core Cadastros — Turnos por unidade** (2026-04-17): `Turno.UnidadeLotacaoId` nullable adicionado, migration idempotente `AddUnidadeLotacaoIdToTurno`, contratos + `TurnoController` com filtros por unidade (`includeGlobals`), UI com select na edição, filtro de unidade na lista, coluna, import/export estendidos. 447/447 testes verdes
- [x] **Core Admin — Logs para admin do tenant** (verificado em 2026-04-17): auditoria revelou que o endpoint já existe em `LoggingController.cs` (`GET /api/logs/entries` com `[RequirePermission("logs.view")]`) e `AdminOperationalLogsScreen.tsx:53` já consome. Isolamento por tenant garantido via `LogEntry : ITenantEntity` + filtro automático do `AppDbContext`. O épico estratégico da visão arquitetural está coberto sem necessidade de endpoint novo
- [x] **Refatoração do Portal MVC (`LioTecnica.Web`) para Next.js — FASE 13 CONCLUÍDA** (2026-04-20, Sessão 25): 7/7 subfases executadas de uma vez. (13.1) saneamento com link `/privacidade` no `LoginScreen.tsx`; (13.2) Entra ID fim-a-fim server-side na API via `EntraChallengeService` (Authorization Code Flow + state HMAC-SHA256 + 23 testes novos em `EntraChallengeServiceTests.cs`); (13.3) congelamento pulado — delete direto; (13.4) `/bff/` removido de `next.config.ts` e `nginx.conf`; (13.5) PortalVagas 100% no Next + **bug colateral corrigido**: `/PortalVagas/Proposta/[token]/page.tsx` refatorado com `generateStaticParams()` + novo `PropostaPublicaPageClient.tsx` (shell+client) para destravar `pnpm build` com `output: "export"`; (13.6) `LioTecnica.Web/` + `LioTecnica.Web.E2E/` + `Dockerfile` raiz apagados do disco; (13.7) `LioTecnica.sln` reescrita sem os GUIDs das duas entradas, restam apenas `Liotecnica.Integration.RM` + `Liotecnica.Integration.RM.Schema`. Scripts (`dev-all.sh`, `test-battery.sh`, `__scripts__/README.md`) e docs (`README.md`, `VISAO_GERAL_PROJETO.md`, `MIGRACAO-RAZOR-PARA-NEXT-ANALISE.md`, `PORTAL_MVC_INVENTARIO_E_MIGRACAO.md`) atualizados. **Regressão: 699/699 backend verde (676 + 23 Entra) + `dotnet build` limpo + `pnpm build` 125/125 páginas + tsc sem erros.** Ver [PORTAL_MVC_INVENTARIO_E_MIGRACAO.md](./PORTAL_MVC_INVENTARIO_E_MIGRACAO.md) §8.
- [x] **Aplicar reposicionamentos dos cadastros órfãos no código** (2026-04-17, decisões da visão §6.4 aplicadas; **sidebar totalmente reescrito na Sessão 24 (2026-04-20)** — hoje tudo vive no `NavegacaoManifest` do backend):
  - Agenda e Entrada ficam no bucket `recrutamento-selecao` via módulos do pacote R&S (não mais via `RECRUTAMENTO_ROUTES`).
  - Feedback (`/gestao/humor`, `/gestao/resumoatividades`) cai em `gestao-pessoas` via `PackageKey` do módulo `feedback` (não mais via `FEEDBACK_ROUTES`/`getModuleKey`).
  - `nav-bloqueiopessoa` e `nav-feedback-gamificacao` não aparecem no `NavegacaoManifest.Items` (código substitui o antigo `permissionManifest.ts`, que foi deletado na Sessão 24).
  - Batida de Ponto, Pagamento Extra e Desligamentos **aparecem travadas** com `motivoBloqueio="pacote-inativo"` no bucket `folha-pagamento` (upgrade em relação ao `HIDDEN_ROUTES` antigo — cadeado em vez de invisível). Reversível em 1 linha: `PackageCatalog.FolhaPagamento.IsActive = true`.
- [x] **Painel de Solicitações — posicionamento decidido** (Onda 14, 2026-04-20): aplicada **opção B1 (transversal core)**. Item movido do bucket `gestao-pessoas` para `principais` no `NavegacaoManifest.cs` (`Destacado: true, Ordem: 40`) e adicionado ao `PRINCIPAIS_ORDER` do `recruitmentNavigation.ts`. Justificativa: agregador analítico multi-tipo (Vaga / Promoção / Desligamento / Férias / Benefício / Dependente / Endereço) que cruza R&S, Folha e Cadastros — colocá-lo em "Gestão de Pessoas" excluiria gestores de R&S/Folha; dividir por pacote (B3) perderia a visão consolidada. Fica ao lado do par workflow Minhas Pendências/Solicitações como "visão consolidada do RH". Permissão preservada: `gestao.dashboard`. 29/29 testes Navegacao passando.

### Operacionais

- [ ] Rodar `fix-broken-migrations.sh` caso apareça erro "relação X já existe" ao subir o app (migration `20260411055438_AddUnidadeLotacaoHierarchyV2` é conhecidamente corrompida)
- [ ] Instalar extensão `pgvector` no Postgres local e executar `AddEmbeddingSupport.sql` para habilitar matching por IA
- [ ] Configurar Python 3.11+ e subir o serviço `RHPortal.Ai` (porta 8000) para validar matching por IA
- [x] **Frontend:Port via configuração — Onda 15 (2026-04-20)**: criadas chaves `Frontend:Port` (default 3005) e `Frontend:BaseUrl` (override completo) no `appsettings.json`; `PreAdmissaoService` agora resolve a URL pública do candidato pelo helper privado `BuildFrontendUrl(path)` (lê BaseUrl override → senão `{request.scheme}://{request.host}:{Port}`); `DevSeedController` adotou o mesmo padrão via `FrontendBaseUrl()`. Hardcodes `:3000`/`:3005` eliminados (`grep -rn ":3000|:3005" RHPortal.Api/RHPortal.Api/**/*.cs` retorna 0). 676/676 testes backend passam.
- [ ] Revisar outros pontos com hardcode de `3000` (dev-core.ps1, dev-all.sh, appsettings do Portal MVC, READMEs) caso a convenção passe a ser `3005`
- [x] **Módulos fase 2 — Gate no backend** (2026-04-20): `[RequireModule("<key>")]` + `ModuleAuthorizationHandler` + `PermissionPolicyProvider` (prefix `Module:`); Owner/ApiKey/core fazem bypass; 14 controllers decorados. 8 testes novos em `Modules/ModuleAuthorizationHandlerTests.cs`.
- [x] **Módulos fase 2 — Bootstrap** (2026-04-20): `DbSeeder.MigrateAndSeedAsync` agora chama `TenantModuleService.EnsureDefaultsAsync(tenantId, ct)` para cada tenant no loop de migrations (idempotente — só insere o que está faltando em `MasterDb.TenantModules`).
- [x] **Logs operacionais no painel do admin** (resolvido há tempos — verificado em 2026-04-17): `AdminOperationalLogsScreen.tsx:53` consome `GET /api/logs/entries` (que já existe em `LoggingController.cs:213`). Backlog estava desatualizado; sem trabalho pendente.

## Concluído

- [x] Clonar repositório Azure DevOps (2026-04-16)
- [x] Configurar connection strings com senha local `@FelipeL89*` (2026-04-16)
- [x] Criar `RHPortal.Ai/.env` com credenciais locais (2026-04-16)
- [x] Remover PAT do remote git (2026-04-16)
- [x] Subir API (5056) + Next.js (3000 → 3005) via `dev-core.sh` (2026-04-16)
- [x] Ajustar CORS e hardcode de porta para 3005 (2026-04-16)
- [x] Criar estrutura de documentação (agent.md, diário, backlog, changelog, documentação) (2026-04-16)
- [x] Criar `habilidades.md` com conhecimentos reutilizáveis do projeto (2026-04-16)
- [x] Documentar função de cada usuário do sistema em `documentacao.md` (2026-04-16)
- [x] Trocar senha padrão dos usuários seedados para `YkmF@2022*` (2026-04-16)
- [x] Corrigir rotas do `TabUsuarios.tsx` do painel do Owner (404 em criar/editar/listar usuários do tenant) (2026-04-16)
- [x] **Módulos fase 1**: entity `TenantModule`, migration, catálogo, service, endpoints no Owner, seed automático, filtro de menus e UI (2026-04-17)
- [x] **Módulos fase 1 — testes**: 29 testes novos (14 `ModuleCatalog` + 15 `TenantModuleService`), 100% aprovados, evidência em `test-evidence/2026-04-17_modulos_por_tenant.md` (2026-04-17)
- [x] **Separação Owner vs Admin do tenant**: removidas 7 abas duplicadas do painel do Owner (Acessos, Menus, Templates email, Emails, Config email, Entra ID, Idioma); corrigidas 2 rotas divergentes (`/api/admin/entra-id` e `/api/admin/localization`) (2026-04-17)
- [x] **Estabilização da suite API**: resolvidas 7 falhas remanescentes (`Funcionarios`, `PreAdmissao`, `SolicitacoesVaga`), regressão completa em 425/425 verde; evidência em `test-evidence/2026-04-17_estabilizacao_suite_api.md` (2026-04-17)
- [x] **Bateria smoke API+AI+Web executada**: 59 cenários (45 pass, 5 fail, 9 skip) com evidência em `test-evidence/2026-04-17_test-battery.md` (2026-04-17)
- [x] **Bateria smoke sem falhas**: ajustes de contrato no `test-battery.sh` (rotas dashboard atualizadas + aceitação `200|403` em endpoints de permissão) e nova rodada com 59 cenários (50 pass, 0 fail, 9 skip) (2026-04-17)
- [x] **Owner logs corrigido (erro de carregamento)**: adicionados endpoints proxy no `OwnerController` para logs transacionais e operacionais (`/api/owner/tenants/{tenantId}/config/logs/*` e `/api/owner/tenants/{tenantId}/config/operational-logs/*`), eliminando `404` nas abas de logs (2026-04-17)
- [x] **Consolidação de documentação para handoff**: sincronizados `agent.md`, `documentacao.md`, `habilidades.md`, `tasks.md`, `backlog.md`, `diario-de-bordo.md` e `changelog.md` para continuidade no Claude Code (2026-04-17)
- [x] **Knowledge Base da sidebar do tenant**: criado guia detalhado de todos os blocos e abas em `knowledge-base/sidebar-blocos-e-abas.md`, com finalidade funcional e notas operacionais de visibilidade/permissão (2026-04-17)
- [x] **Visão arquitetural TO-BE**: capturada em entrevista e consolidada em `knowledge-base/visao-arquitetural.md` — entitlement em dois níveis (pacotes + módulos), taxonomia completa (R&S, Gestão de Pessoas, Folha, Cadastros core, Admin core, Relatórios transversal), conceitos de domínio novos e backlog estratégico (2026-04-17)
- [x] **Fase 0 — loop de entitlement fechado**: `TenantPackageServiceTests` (17 testes) + composição pacote↔módulo em `TenantModuleServiceTests` (+5 testes) + `TenantModuleResponse.PackageKey` + UI Owner `TabModulos.tsx` com toggle master por pacote. 447/447 testes verdes, tsc limpo (2026-04-17)

## Bloqueado

_(nenhum item)_
