################################################################################
# Este arquivo .gitignore foi criado automaticamente pelo Microsoft(R) Visual Studio.
################################################################################

/.vs/LioTecnica/CopilotIndices/17.13.444.19527/CodeChunks.db
.vs/
**/bin/
**/obj/
*.docx

# macOS metadata — nunca vai para o repo
.DS_Store
**/.DS_Store

# Arquivos de ambiente sensíveis
.env
.env.local
.env.qa
.env.prod
**/.env
# Exceção: .env na raiz é template com placeholders (editar no servidor)
!/.env

# appsettings.Local.json: nunca ir para o repo.
**/appsettings.Local.json

# appsettings.Development.json: decisão deliberada (sessão 2026-04-24) — versionar
# no repo com senhas de DEV incluídas, até a próxima rotação. Trocar quando
# aplicável e remover este bypass. Em produção, nunca fazer isso.
# **/appsettings.Development.json  ← DESATIVADO INTENCIONALMENTE
!**/appsettings.Development.sample.json

# Arquivos problemáticos no Windows
nul
**/nul
Liotecnica.Integration.RM.Logs/extraction.log

# Python bytecode/cache
**/__pycache__/
*.pyc
